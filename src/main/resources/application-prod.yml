logging:
  level:
    root: INFO
    org.hibernate.SQL: INFO

myth-cloth:
  facebook:
    app-id: "4393993617525351"
    app-secret: "50f71fa2dbdb2acb02eef826ae10c110"
    graph-url: https://graph.facebook.com/
  google:
    client-id: 1068577777042-9ftlmmtq40sbtngopmng5ks2eqq5hef8.apps.googleusercontent.com

  security:
    jwt:
      secret: "ug5RzW3B4A46J3nrRw6XHgJw1RlGw9oAYOAtdc6bR8c="
      # Use the following command to generate a new secret:  `openssl rand -base64 32`
      issuer: "myth-cloth-api"
      ttl-minutes: 60

server:
  port: 8080

spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true   # <- Pretty-print SQL
        show_sql: true     # <- Log SQL to console
        use_sql_comments: false # <- Adds Hibernate comments to queries
